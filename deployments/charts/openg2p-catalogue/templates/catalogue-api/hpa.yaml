{{- if and .Values.catalogueAPI.enabled .Values.catalogueAPI.autoscaling.enabled }}
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ include "catalogue.apiName" . }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ include "catalogue.apiName" . }}
  minReplicas: {{ .Values.catalogueAPI.autoscaling.minReplicas }}
  maxReplicas: {{ .Values.catalogueAPI.autoscaling.maxReplicas }}
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: {{ .Values.catalogueAPI.autoscaling.targetCPUUtilizationPercentage }}
{{- end }}
